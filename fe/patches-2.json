{
  "baseSha256": "ec9eda9c3b761be3bf5c4c138ebb258878ed383ad6461d345036141620649d9d",
  "expectedSha256": "f6a0c7ce38a49ff4a36c381547ab33e7240d1373c5d2154baef918827c9e167b",
  "builtAt": "2026-08-10 monthly close: not-needed-this-month skip",
  "patches": [
    {
      "note": "Monthly Close: skip-month helpers and the password-gated Not-needed action",
      "find": "  window.mcSetComment = function (id, v) {\n",
      "replace": "  // \"Not needed this month\": a new or dormant apartment with nothing to clear.\n  // Password-gated so it cannot be used to quietly bypass a real clearing, and\n  // recorded so the FOLLOWING month shows that this one was skipped.\n  var MC_SKIP_PW = '2026';\n  function mcPrevMonth(m) {\n    var p = String(m || '').split('-');\n    var d = new Date(parseInt(p[0], 10), parseInt(p[1], 10) - 2, 1);\n    return d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0');\n  }\n  function mcSkipRec(id, month) {\n    var st = (S.monthlyClose || {})[month || mcMonth];\n    var r = st && st[String(id)];\n    return (r && r.skipped) || null;\n  }\n  function mcSkipped(id) { return !!mcSkipRec(id); }\n  function mcPrevSkipped(id) { return mcSkipRec(id, mcPrevMonth(mcMonth)); }\n  window.mcNotNeeded = function (id) {\n    var a = apts().filter(function (x) { return x.id === String(id); })[0]; if (!a) return;\n    var pw = prompt('Mark \"' + a.name + '\" as NOT needing a clearing for ' + mLabel(mcMonth) + '?\\n\\nEnter the password to confirm:');\n    if (pw === null) return;\n    if (String(pw).trim() !== MC_SKIP_PW) { toast('Wrong password - the apartment was not skipped.', 'err'); return; }\n    if (a.type === 'private') {\n      if (!confirm('Reminder: ' + a.name + ' is a PRIVATE apartment.\\n\\nThe TAKK still has to be issued and submitted for ' + mLabel(mcMonth) + ' even though no clearing is sent.\\n\\nContinue and skip the clearing?')) return;\n    }\n    var note = prompt('Reason (optional) - shown on next month\\'s flag:', 'New apartment - not ready to clear');\n    var rec = mcRec(String(id), true);\n    rec.skipped = { at: Date.now(), by: who(), reason: String(note || '').trim(), takkDue: a.type === 'private' };\n    save(); renderMt();\n    toast(a.name + ' marked as not needing a clearing for ' + mLabel(mcMonth) + '.', 'ok');\n  };\n  window.mcUndoNotNeeded = function (id) {\n    var rec = mcRec(String(id), true);\n    if (!rec.skipped) return;\n    if (!confirm('Put this apartment back into the ' + mLabel(mcMonth) + ' queue?')) return;\n    delete rec.skipped; save(); renderMt();\n  };\n  window.mcSetComment = function (id, v) {\n"
    },
    {
      "note": "Monthly Close: apartments marked not-needed leave the queue",
      "find": "  function queue() { return pool().filter(function (a) { return !complete(a); }); }",
      "replace": "  function queue() { return pool().filter(function (a) { return !complete(a) && !mcSkipped(a.id); }); }"
    },
    {
      "note": "Monthly Close: not-needed apartments counted separately, never as sent",
      "find": "    all.forEach(function (a) {\n      var n = (a.members || [a.id]).length; tot += n;\n      if (complete(a)) done += n; else if (started(a)) doing += n;\n    });",
      "replace": "    var skipN = 0;\n    all.forEach(function (a) {\n      var n = (a.members || [a.id]).length; tot += n;\n      if (mcSkipped(a.id)) { skipN += n; return; }   // not needed this month\n      if (complete(a)) done += n; else if (started(a)) doing += n;\n    });"
    },
    {
      "note": "Monthly Close: progress and pace measure what actually needs clearing",
      "find": "    var pct = tot ? Math.round(done / tot * 100) : 0;\n    // Pace to target:",
      "replace": "    var _need = Math.max(0, tot - skipN);   // apartments that actually need clearing\n    var pct = _need ? Math.round(done / _need * 100) : 100;\n    // Pace to target:"
    },
    {
      "note": "Monthly Close: pace counts only apartments that still need clearing",
      "find": "      var left = tot - done;",
      "replace": "      var left = _need - done;"
    },
    {
      "note": "Monthly Close: legend shows the not-needed tally",
      "find": "' + (tot - done - doing) + ' not started</span>' + _pace + '</div></div>'",
      "replace": "' + (tot - done - doing - skipN) + ' not started</span>' + (skipN ? '<span><i class=\"mcd todo\" style=\"background:#94A3B8\"></i>' + skipN + ' not needed</span>' : '') + _pace + '</div></div>'"
    },
    {
      "note": "Monthly Close: focus card flags an apartment whose previous month was skipped",
      "find": "          + '<button class=\"mc-flag' + (r && r.flag ? ' on' : '') + '\" onclick=\"mcFlag(\\'' + esc(cur.id) + '\\')\" title=\"Flag\">&#9873;</button></div>'",
      "replace": "          + (function () { var ps = mcPrevSkipped(cur.id); return ps\n            ? '<span title=\"' + esc(ps.reason || '') + (ps.takkDue ? ' (TAKK was still due)' : '') + '\" style=\"background:#FEF3C7;border:1px solid #D97706;color:#92400E;border-radius:99px;padding:2px 9px;font-size:10px;font-weight:600\">&#9888; ' + esc(mLabel(mcPrevMonth(mcMonth))) + ' skipped</span>'\n            : ''; })()\n          + '<button class=\"mc-flag' + (r && r.flag ? ' on' : '') + '\" onclick=\"mcFlag(\\'' + esc(cur.id) + '\\')\" title=\"Flag\">&#9873;</button></div>'"
    },
    {
      "note": "Monthly Close: Not-needed button on the focus card",
      "find": "          + (Q.length > 1 ? '<button class=\"btn sm\" onclick=\"mcSkip()\">Skip</button>' : '')",
      "replace": "          + (Q.length > 1 ? '<button class=\"btn sm\" onclick=\"mcSkip()\">Skip</button>' : '')\n          + '<button class=\"btn sm\" onclick=\"mcNotNeeded(\\'' + esc(cur.id) + '\\')\" title=\"This apartment has nothing to clear this month - password required\">Not needed this month</button>'"
    },
    {
      "note": "Monthly Close: list view shows the not-needed state with an undo, and the previous-month flag",
      "find": "          + (cp ? '<span class=\"mc-sent\">&#10003; Sent</span>'",
      "replace": "          + (mcPrevSkipped(a.id) ? '<span title=\"previous month was marked not needed\" style=\"color:#D97706;font-size:11px;margin-right:6px\">&#9888;</span>' : '')\n          + (mcSkipped(a.id) ? '<span style=\"font-size:11px;color:var(--tx3)\">Not needed <button class=\"btn sm\" onclick=\"mcUndoNotNeeded(\\'' + esc(a.id) + '\\')\" style=\"margin-left:6px\">Undo</button></span>'\n            : cp ? '<span class=\"mc-sent\">&#10003; Sent</span>'"
    },
    {
      "note": "Monthly Close: all-clear banner acknowledges skipped apartments",
      "find": "<div class=\"mc-ads\">Nothing left in the queue.</div></div>';",
      "replace": "<div class=\"mc-ads\">Nothing left in the queue.' + (skipN ? ' ' + skipN + ' apartment' + (skipN === 1 ? '' : 's') + ' marked as not needed this month.' : '') + '</div></div>';"
    }
  ]
}
